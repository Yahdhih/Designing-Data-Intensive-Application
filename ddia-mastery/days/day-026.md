# Jour 026 — Phase 1 · Re-dériver LSM vs B-tree de mémoire

> ⏱ 60 min  |  🛠️ Re-dérivation 45 · 🧠 Vérification 15
> 💡 **Rappel méthode — Double codage :** Travailler avec du texte seul n'ancre pas suffisamment. Dessine les schémas (memtable, WAL, SSTables, pages B-tree) sur papier ou dans un éditeur de schémas. La mémoire spatiale vient renforcer la mémoire sémantique.

---

## 🔁 Réveil mémoire — 5 min

- **Page blanche :** De mémoire — quelles 9 entrées de `reference/tradeoffs.md` as-tu complétées hier ? Cite les 3 moteurs de stockage + leurs amplifications respectives.
- **Cartes Anki :** `ch03::storage` — balaye toutes les cartes dues.

---

## 🛠️ Re-dérivation de mémoire — 45 min

> **Règle :** Tout ce qui suit se fait **sans ouvrir le livre, sans regarder tes notes, sans relire les fichiers `code/`**. Utilise uniquement ce que tu as en tête. À la fin, tu compareras avec la vérité.

### Partie 1 — Re-dessiner les schémas (15 min)

Dessine sur papier (ou dans `reference/notes/jour-026-schemas.md`) :

**Schéma 1 — LSM-tree : chemin d'une écriture**
```
[Écriture] → [___] → [___] → [___]
                              ↓ (déclenché quand)
                           [Compaction : ___→___]
```
Remplis : Quels composants ? Quand le flush est-il déclenché ? Quel est le but de la compaction ?

**Schéma 2 — Lecture dans un LSM-tree**
```
[Requête clé K] → Chercher dans [1. ___] → si absent : [2. ___] → ... → [n. ___]
Optimisation : [___] pour éviter les fausses lectures
```

**Schéma 3 — B-tree : mise à jour in-place**
```
[Page feuille]     [Page interne]     [Racine]
  ↑                    ↑                 ↑
[Écriture] → [WAL d'abord] → [Modifier la page ___]
              (pourquoi ?)     (pourquoi in-place ?)
```

**Schéma 4 — Stockage en colonnes**
```
Ligne-oriented :           Colonne-oriented :
[id|nom|age|score]    →   [col_id: 1,2,3,...] [col_nom: A,B,C,...] ...
                           ↓ avantage OLAP :
                           [compression ___] [vectorisation ___]
```

### Partie 2 — Règles de choix (15 min)

Remplis ce tableau **de mémoire** dans `reference/notes/jour-026-regles.md` :

| Critère | LSM-tree | B-tree |
|---------|----------|--------|
| Amplification écriture | | |
| Amplification lecture | | |
| Amplification espace | | |
| Range scan | | |
| Workload idéal | | |
| Exemples de systèmes | | |
| Mode de défaillance principal | | |

Et ces questions :
1. Pourquoi Cassandra utilise-t-il un LSM-tree plutôt qu'un B-tree ? (3 raisons)
2. Pourquoi Postgres utilise-t-il un B-tree comme index par défaut ?
3. Pourquoi le stockage en colonnes est-il plus adapté à OLAP qu'OLTP ?
4. Quel est l'avantage d'un filtre de Bloom dans un LSM-tree avec beaucoup de SSTables ?

### Partie 3 — Code de mémoire (15 min)

Écris **de mémoire** les signatures des fonctions clés de tes jouets :

**Bitcask (`code/01-storage-retrieval/bitcask.py`) :**
```python
class Bitcask:
    def put(self, key: str, value: str) -> None: ...  # que fait-on ?
    def get(self, key: str) -> str | None: ...        # que fait-on ?
    def compact(self) -> None: ...                    # étapes ?
```

**MiniLSM (`code/01-storage-retrieval/lsm.py`) :**
```python
class MiniLSM:
    def put(self, key: str, value: str) -> None: ...  # que fait memtable.put ?
    def get(self, key: str) -> str | None: ...        # ordre de recherche ?
    def _flush(self) -> None: ...                     # quand ? que génère-t-on ?
```

**MiniB-tree (`code/01-storage-retrieval/btree.py`) :**
```python
class BTree:
    def insert(self, key: int, value: str) -> None: ...   # que fait _split_child ?
    def range_scan(self, lo: int, hi: int) -> list: ...   # pourquoi B-tree ≠ hash ?
```

---

## 🧠 Vérification — 15 min

**Maintenant** ouvre les fichiers de référence et compare :

- [ ] **Rouvre `code/01-storage-retrieval/bitcask.py`** — compare les schémas et le code que tu as re-dérivé avec la réalité. Quelles différences ?
- [ ] **Rouvre `code/01-storage-retrieval/lsm.py`** — idem.
- [ ] **`reference/tradeoffs.md`** — compare ton tableau de règles de choix avec les entrées existantes.
- [ ] **Journalise les écarts dans `reference/notes/jour-026-ecarts.md`** :
  - Ce que j'avais oublié / faux :
  - Ce que j'avais bien mémorisé :
  - Concept à renforcer par une carte ciblée :

- [ ] **Si tu as trouvé des erreurs dans tes jouets lors de la comparaison**, corrige maintenant — c'est l'occasion rare de trouver des bugs en revenant avec un regard neuf.

---

## ✅ Terminé quand

- [ ] 4 schémas dessinés de mémoire
- [ ] Tableau LSM vs B-tree complété de mémoire
- [ ] Signatures de code re-dérivées de mémoire
- [ ] Écarts documentés dans `reference/notes/jour-026-ecarts.md`
- [ ] PROGRESS.md coché ✅

## ↪ Demain
Consolidation Phase 1 complète : tu enseignes Ch.1-4 à voix haute (technique Feynman), puis tu résumes chaque chapitre en une seule « loi fondamentale ».
